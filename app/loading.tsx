import { LoadingUI } from "@/components/loading";

export default function Loading() {
  return (
    <section className="flex items-center justify-center mx-auto my-52">
      <LoadingUI />
    </section>
  );
}
